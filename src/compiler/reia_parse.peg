%% Grammar

add_expr <- integer add_op add_expr / integer
`
case Node of
  [Left, Op, Right] -> {binary_op, line(Idx), list_to_atom(Op), Left, Right};
  _ -> Node
end
`;

add_op  <- "+" / "-";

integer <- [0-9]+ `{integer, line(Idx), list_to_integer(Node)}`;

%% Extra functions
`line({{line, Line},_}) -> Line.`